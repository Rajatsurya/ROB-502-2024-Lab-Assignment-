
#include <iostream>

int main() {// we have to define the type to the function as well
    // Correctly declare and initialize the variable
    int i = 0; // here we are assigning memory 

    // Use a for-loop in C++ style
    for (i = 1; i <= 10; i=i+1) { // Starting from 1 to 10
        std::cout << "my name is rajat " << i << "\n"; // Correct way to output with variables
    }

    return 0;
}
